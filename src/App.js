import Section from "./components/section/section.components";

const App = () => {
  return (
      <div className="container mx-auto flex flex-col items-center justify-center max-h-screen h-screen border-box bg-gray-600 w-full">
        <Section/>
      </div>
  );
}

export default App;
